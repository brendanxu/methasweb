{
  "name": "@methas/design-tokens",
  "version": "1.0.0",
  "description": "Design tokens for METHAS enterprise website",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npx sass src/tokens.scss dist/tokens.css && npx sass src/index.css dist/index.css",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "sass": "^1.77.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*",
    "src/**/*"
  ]
}